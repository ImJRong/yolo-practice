# YOLO 学习日志


---

## 2026-09-21（上午·YOLO 检测篇）

### 一、学到了什么

**1. YOLO 检测一张图的代码**

```python
from ultralytics import YOLO

model = YOLO("yolov8m.pt")       # 加载模型
results = model("图片.jpg")       # 检测

for r in results:
    for box in r.boxes:
        print(model.names[int(box.cls)])   # 类别名，如 person
        print(float(box.conf))             # 置信度，0~1
        print(box.xyxy)                    # 框坐标
```

**2. 结果对象的层级关系**

```
results    列表，装所有图片的结果
  └ r      一张图的结果
      └ r.boxes    这张图里所有的框
          └ box    其中一个框
```

**3. 三个常用属性**

| 写法 | 意思 |
|---|---|
| `model.names[int(box.cls)]` | 类别名（person / car / dog...） |
| `float(box.conf)` | 置信度，越接近 1 越确定 |
| `box.xyxy` | 框的位置，格式 `[[x1, y1, x2, y2]]` |

**4. 为什么要加 `int()` 和 `float()`**

`box.cls` 打印出来是 `tensor(0.)`，不是普通的数字 `0`。
这是 PyTorch 的张量类型，不能直接当字典的键用，要先转成整数：

```python
int(box.cls)      # 变成 0，才能写 model.names[0]
float(box.conf)   # 变成 0.87，才能参与计算
```

**记住**：只有一个数的张量才能这么转。多个数的用 `.tolist()`。

**5. 参数怎么调**

```python
model("图.jpg", classes=[0])   # 只检测人，其他类别不显示
model("图.jpg", conf=0.5)      # 只保留置信度 0.5 以上的框
```

- `classes=[0]` → 人就是编号 0。想看有哪些编号，`print(model.names)`
- `conf` 调高 = 误检少但漏检多，调低 = 反之

**6. 多张图一起检测**

```python
results = model(["1.jpg", "2.jpg", "3.jpg"])

for i, r in enumerate(results):
    r.save(filename=f"result_{i}.jpg")     # 用 i 加序号，否则互相覆盖
```

---

### 二、踩过的坑

| 坑 | 正确做法 |
|---|---|
| 终端报 `No module named 'ultralytics'` | 先看提示符有没有 `(yolo)`，没有就是环境没激活 |
| 多张图存结果，文件互相覆盖 | 用 `enumerate` 生成 `result_0.jpg` `result_1.jpg` |
| 模型识别不准 | 正常现象。小目标、遮挡会漏。不是代码错 |
| 图片被传到 GitHub | `.gitignore` 里加 `*.jpg` `*.png`，已传的要 `git rm --cached` |

---

### 三、备忘

- 项目目录：`D:\yolo-practice`
- 跑代码前确认终端提示符是 `(yolo)`，不是 `(base)` 也不是空的
- 仓库：`git@github.com:ImJRong/yolo-practice.git`（Private）
- 私人照片不上传 GitHub

---

### 四、下一步

**OpenCV 基础** —— 理解"图片就是一张数字表格"

新建 `learn_cv.py`，自己敲：

```python
import cv2

img = cv2.imread("bus.png")
print("形状:", img.shape)
print("类型:", img.dtype)
print("左上角:", img[0, 0])
print("中心:", img[157, 237])
```

要搞明白：

1. `img.shape` 出来几个数，分别是什么
2. `img[0, 0]` 出来几个数
3. 为什么是 3 个数

**核心**：图片不是图片，是一张巨大的数字表格。每个格子一个像素，3 个数代表蓝、绿、红三个颜色的亮度。

---

## 2026-09-21（下午·OpenCV 篇）

### 一、学到了什么

**1. 图片 = 数字表格**

```python
img = cv2.imread("bus.png")
print(img.shape)     # (315, 474, 3)
print(img.dtype)     # uint8
print(img[0, 0])     # [203 189 191]
```

- `(315, 474, 3)` = **高 315 行、宽 474 列、每格 3 个数**
- `uint8` = 每个数 0~255，占 1 字节
- 每格 3 个数 = **蓝、绿、红**（注意顺序！）

**2. 索引顺序：`img[行, 列]` = `img[y, x]`**

```
第一个位置 = 行 = 上下方向 = y
第二个位置 = 列 = 左右方向 = x
```

⚠️ **YOLO 给的是 `(x, y)`，跟 numpy 索引反着！**

```python
img[0, 400]     # ✅ 第0行、第400列 → 上边缘靠右
img[400, 0]     # ❌ 报错，行只有315
```

报错信息 `index 400 is out of bounds for axis 0 with size 315` 里的
**axis 0** 就是第一个维度（行）。

**3. BGR 不是 RGB**

`img[0,0]` = `[203 189 191]`，第一个数是**蓝色**。
OpenCV 历史遗留，用 BGR 顺序。用 matplotlib 显示会红蓝颠倒。

**4. 切片（取一块区域）**

```python
img[100:105, 200:205, 0]     # 第100~104行、第200~204列、蓝色通道
```

- **左闭右开**：`100:105` 取 100,101,102,103,104 五个，**不含 105**
- **起点必须小于终点**，写 `100:95` 得到空的
- 加了第三个下标就是选通道：`0`=蓝 `1`=绿 `2`=红

**5. 常用函数**

```python
cv2.resize(img, (200, 133))              # 缩放，参数是 (宽, 高)
cv2.imwrite("out.png", img)              # 保存，失败返回 False 不报错
cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)    # 转灰度
cv2.rectangle(img, (x1,y1), (x2,y2), (0,0,255), 5)   # 画矩形
```

**`resize` 的坑**：参数是 `(宽, 高)`，但 `shape` 是 `(高, 宽, 3)`，**反着**。

**6. 灰度图怎么来的**

每格 3 个数压成 1 个：

```
灰度 = 0.299×R + 0.587×G + 0.114×B
```

绿权重最高，因为人眼对绿色最敏感。

结果：shape 从 `(315,474,3)` 变成 `(315,474)`，通道那一维没了。

**7. 张量剥壳：`box.xyxy[0]`**

```python
box.xyxy         # tensor([[12.3, 45.6, 200.1, 300.7]])   ← 两层括号
box.xyxy[0]      # tensor([12.3, 45.6, 200.1, 300.7])    ← 一层，4个数
```

**规律：看 `.shape` 有几个数，就要剥几次。** `(1,4)` → 剥一次 → `(4,)`。

**8. YOLO 坐标属性**

```python
r.boxes.xyxy     # [x1, y1, x2, y2] 左上+右下
r.boxes.xywh     # [cx, cy, w, h]   中心+宽高
r.boxes.xyxyn    # 归一化版（0~1）
r.boxes.conf     # 置信度
r.boxes.cls      # 类别编号
```

`r.boxes` 的 shape 是 `(N, 4)`，N = 检测到几个物体。

**9. 用 OpenCV 自己画 YOLO 的框**

```python
from ultralytics import YOLO
import cv2

img = cv2.imread("bus.png")
model = YOLO("yolov8m.pt")
result = model("bus.png", classes=[5])[0]

for b in result.boxes:
    x1 = int(b.xyxy[0][0])
    y1 = int(b.xyxy[0][1])
    x2 = int(b.xyxy[0][2])
    y2 = int(b.xyxy[0][3])

    cv2.rectangle(img, (x1, y1), (x2, y2), (0, 0, 255), 5)

cv2.imwrite("bus_cv.png", img)
```

**注意**：`cv2.rectangle` 的参数是 `(x1,y1)` `(x2,y2)` = `(列,行)`，
跟你手画时的 `img[行,列]` **反着**。

**10. 手画四条边（练索引用，实战别这么写）**

```python
img[y1:y1+5, x1:x2] = [0,0,255]     # 上边
img[y2-5:y2, x1:x2] = [0,0,255]     # 下边
img[y1:y2, x1:x1+5] = [0,0,255]     # 左边
img[y1:y2, x2-5:x2] = [0,0,255]     # 右边
```

**记法**：
- **竖线**（左右）→ 行跨全程，列 5 像素
- **横线**（上下）→ 列跨全程，行 5 像素

**11. COCO 常见类别编号**

```
0  person      1  bicycle     2  car
3  motorcycle  4  airplane    5  bus
6  train       7  truck       8  boat
```

看全部：`print(model.names)`

---

### 二、踩过的坑

| 坑 | 正确做法 |
|---|---|
| `imread` 读不到文件**不报错**，返回 `None`，后面 `.shape` 才崩 | 加 `if img is None: exit()` 检查 |
| 图片明明在，却读不到 | **相对路径是相对于"敲命令的目录"**，不是脚本所在目录。用绝对路径或先 `cd` |
| `img[400,300]` 越界 | 先看 `img.shape`，行数别超 |
| 切片写成 `y1:y1-5` | 起点必须小于终点，应是 `y1:y1+5` |
| `img[x1:x2, y1:y2]` 画歪 | 第一个位置是**行(y)**，应该是 `img[y1:y2, x1:x2]` |
| 直接 `int(b.xyxy[0])` 报错 | `xyxy` 是 4 个数，要 `int(b.xyxy[0][0])` 逐个取 |

---

### 三、备忘

- 学习目录：`D:\yolo-practice\learn_cv`
- 测试图：`bus.png`（315×474）
- 清屏用 `cls` 或 `Ctrl+L`，**别用 `Ctrl+C`**（那是中断程序）
- 生成的文件：`small.png` `crop.png` `gray.png` `bus_cv.png`
- 提交代码前看一眼：`git status`，图片会被 `.gitignore` 挡住

---

### 四、下一步

**1. 给框加文字标签**

```python
cv2.putText(img, "bus 0.87", (x1, y1-8),
            cv2.FONT_HERSHEY_SIMPLEX, 0.6, (0,0,255), 2)
```

- `y1-8` = 往上挪 8 像素，不然文字压在框线上
- 文字位置 `(x, y)` 是**左下角**，不是左上角

**2. 后面的大方向**

| 顺序 | 内容 |
|---|---|
| 1 | 训练自己的模型（装 LabelImg 标图 → 写 data.yaml → `model.train()`） |
| 2 | 模型导出部署（ONNX） |
| 3 | 边缘设备部署（Jetson / 树莓派） |

**3. 暂时跳过的东西**

- **列表推导式** `[int(v) for v in ...]` —— 语法糖，看不懂就用 for 循环展开，不影响写代码

---

## 2026-09-22（上午·视频检测篇）

### 一、学到了什么

**1. 视频抽帧：`%` + `continue`**

```python
if n % 10 != 0:
    n = n + 1
    continue
```

- `%` = 取余数，`n % 10 != 0` = "帧号不是 10 的倍数"
- `continue` = **跳过本轮剩下的代码**，直接下一轮
- 效果：只处理 0,10,20...300 共 31 帧

**为什么抽帧**：305 帧全跑要 70~90 秒，抽帧后 9 秒；30fps 相邻帧几乎一样，看检测结果够用。

**⚠️ 递增必须写在 `continue` 之前**，否则被跳过的帧 `n` 不增加 → `n` 永远卡住。

**2. 视频信息怎么读**

```python
cap = cv2.VideoCapture("data/test1.mp4")
w = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))    # 1080
h = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))   # 1920（竖屏）
fps = cap.get(cv2.CAP_PROP_FPS)               # 30.0
```

实测：`test1.mp4` = 305 帧、30fps、10.2 秒、1080×1920。

**3. 写视频三件套**

```python
fourcc = cv2.VideoWriter_fourcc(*"mp4v")
out = cv2.VideoWriter("output/video_yolo/test1.mp4", fourcc, fps, (w, h))
out.write(frame)      # 每帧塞进去
out.release()         # 必须调，不然文件是坏的
```

- `(w, h)` 是**宽高**，跟 `imread` 的 `shape`（高宽）反着
- `out.release()` 不调 → 文件不完整、播不了
- 只存图片的话不需要 `out`，只要 `cap.release()`

**4. ⚠️ `mp4` 是容器，不是编码**

`test1.mp4` 后缀是 mp4，但里面的视频流编码是 **`mp4v`**（MPEG-4 Part 2），
Windows 自带"电影和电视"不支持 → 双击没反应。**换后缀没用**，要么换播放器（PotPlayer/VLC），
要么把 `mp4v` 改成 `avc1`（H.264）。

**5. 参数提到文件顶部当配置**

```python
line_w = 1     # 线粗
word_w = 1     # 字粗
word_s = 0.4   # 字号
red  = (0,0,255)
```

想调就改一行，不用翻长参数列表。

**6. `fontScale` 是缩放系数，不是像素高**

`0.4` ≈ 10 像素高，`1.0` ≈ 26 像素高。**跟图片尺寸无关**，换大图不会自动变大，要手动调。

**7. YOLO 既能吃路径也能吃数组**

```python
model("data/car.png")    # 自己读文件
model(img)               # 喂已经读好的 numpy 数组，省一次读取
model(frame)             # 视频里就是这样
```

**8. COCO 类别：`bus`=5，`car`=2**

换图必须换 `classes`，否则零检测。看全部：`print(model.names)`

**9. `imwrite` 返回布尔值**

```python
ok = cv2.imwrite("out.png", img)   # True 或 False，不是图片
if not ok:
    print("保存失败")
```

---

### 二、踩过的坑

| 坑 | 正确做法 |
|---|---|
| 抽帧里 `continue` 前忘了 `n = n + 1` | 递增写在 `continue` 之前 |
| 存图写成 `img_cv = cv2.imwrite(...)` | 返回的是 `True`/`False`，不是图片 |
| 换了图忘了改 `classes` | `bus.png`→`[5]`，`car.png`→`[2]` |
| `fontScale` 当像素高用 | 是缩放系数，`0.4`≈10px，大图要调到 0.8~1.0 |
| mp4 文件点不开 | 后缀是容器、编码才是关键。`mp4v` 换播放器播 |
| `VideoWriter` 输出目录不存在 | **不报错、静默失败**，生成空文件。目录要手动建 |

---

### 三、备忘

- 清屏 `cls` 或 `Ctrl+L`；关终端 `exit` 或 `Ctrl+Shift+W`
- VS Code 终端垃圾图标 = 关掉重开，会回到项目根目录
- 详细代码讲解：`代码讲解_视频与画框.md`
- 输出目录统一 `output/video_yolo/`（原来拼错成 `vedio`）

---

### 四、下一步

**1. 视频实时播放版**（不存文件，边播边画）

```python
cv2.imshow("video", frame)
if cv2.waitKey(1) & 0xFF == ord("q"):
    break
cv2.destroyAllWindows()
```

**2. 训练自己的模型（B 方向）**

装 LabelImg → 标 30 张图 → 写 `data.yaml` → `model.train(...)`

---

## 2026-09-22（下午·实时播放 + 训练准备）

### 一、学到了什么

**1. 视频实时播放（不存文件，边播边画）**

跟上午的存文件版（`video_detect.py`）区别就三处：

```python
cv2.imshow("video", frame)                     # 弹窗显示这一帧

if cv2.waitKey(1) & 0xFF == ord("q"):          # 按 q 退出
    break

cv2.destroyAllWindows()                        # 循环结束后关窗口
```

不建 `VideoWriter`，每帧直接 `imshow` 显示。

**2. `cv2.waitKey(1)` 拆开看**

```python
cv2.waitKey(1)          # 干两件事：①让画面刷新出来 ②检查这1毫秒有没有按键
& 0xFF                  # 只留最低8位，屏蔽高位杂数据（跨平台保险写法）
== ord("q")             # ord 把字符转成数字编码，q → 113
```

- 参数 `1` = 等 1 毫秒。给 `0` 会一直卡住等按键。
- `waitKey` 返回按键的 ASCII 码，没按键返回 -1。
- 没按键时 `-1 & 0xFF = 255`，不等于 113，条件为假，继续播。

**3. 窗口尺寸适配**

视频比屏幕高（比如竖屏 1080×1728），`imshow` 会切掉一部分。显示前缩小：

```python
h = frame.shape[0]      # 高
w = frame.shape[1]      # 宽

new_h = int(h * 500 / w)                    # 宽固定 500，高按比例算
frame = cv2.resize(frame, (500, new_h))     # 注意参数是 (宽, 高)
```

- `resize` 参数是 `(宽, 高)`，跟 `shape` 的 `(高, 宽)` 反着。
- 缩放只影响显示，**不影响检测精度**（检测用的是原图）。

通用做法：取 `min(MAX_H/h, MAX_W/w)` 当缩放比例，横竖屏都适配。

**4. 模型选型：yolov8m → n**

CPU 上跑 yolov8m 一帧约 200ms，视频要求 33ms/帧，卡成幻灯片。

| 模型 | 速度 | 精度 |
|---|---|---|
| yolov8m | 慢（~200ms） | 高 |
| yolov8s | 中（~90ms） | 中 |
| yolov8n | 快（~40ms） | 略低 |

换小模型 = 牺牲一点精度换速度。**这就是"模型选型权衡"，端侧部署的核心。**

**5. 训练自己的模型：准备三件套**

流程：`标图 → data.yaml → model.train()`

目录结构（YOLO 的约定）：

```
data/lane/
├── images/   放图片
└── labels/   放标注文件（LabelImg 生成）
```

文件名一一对应：`images/lane_01.jpg` ↔ `labels/lane_01.txt`。

标注工具 LabelImg：
- 启动：`labelImg D:/yolo-practice/data/lane/images D:/yolo-practice/data/lane/labels`
- **必须把格式从 PascalVOC 切成 YOLO**（否则存 .xml，YOLO 不认）
- 勾上 `View → Auto Save Mode`

**6. 车道线数据集**

两个主流基准：

| 数据集 | 大小 | 场景 |
|---|---|---|
| TuSimple | ~10GB | 高速白天，简单 |
| CULane | ~40GB | 城市复杂，8 种挑战（夜/雨/遮挡） |

师兄论文用的 CULane。但这些数据集的标注是"折线"格式，不是 YOLO 的框，直接训练要先写脚本转换。

### 二、踩过的坑

| 坑 | 正确做法 |
|---|---|
| `cv2.imread` 读不了中文路径 | OpenCV 内部走 C++ `fopen`，不认中文。用 `np.fromfile` + `cv2.imdecode` |
| 视频竖屏显示不全 | 显示前 `resize` 缩小，只影响显示不影响检测 |
| LabelImg 默认存 .xml | 启动后先点 PascalVOC 按钮切成 YOLO |

**中文路径的完整解法**：

```python
import numpy as np
import cv2

raw = np.fromfile("中文路径/图.jpg", dtype=np.uint8)   # 按字节读整个文件
img = cv2.imdecode(raw, cv2.IMREAD_COLOR)              # 从字节流解码成图

# 写也一样
ok, buf = cv2.imencode(".jpg", img)                    # 编码成字节流
buf.tofile("中文路径/图.jpg")                          # 直接写盘
```

**为什么 imread 会挂**：`cv2.imread` / `cv2.imwrite` 内部调用 C++ 的 `fopen`，
它只认 ANSI 路径，遇到中文（Windows 默认 UTF-16）就读不到。`np.fromfile` 走的是
Python 层，能正确传 Unicode 路径。

### 三、备忘

- 实时播放脚本：`src/video_play.py`（通用版，配置区改 VIDEO / MODEL / MAX_W / MAX_H）
- 数据集规整脚本：`src/dataset_fix_lane.py`（桌面图 → data/lane/images，统一 jpg + 重命名）
- 28 张车道图已就绪：`data/lane/images/lane_01.jpg` ~ `lane_28.jpg`
- 标图用 `W` 键画框，`D` 下一张，`A` 上一张

### 四、下一步

**1. 标注 28 张图**

```
labelImg D:/yolo-practice/data/lane/images D:/yolo-practice/data/lane/labels
```

切 YOLO 格式 → `W` 画框 → 框住车道线。

**2. 写 data.yaml**

**3. 训练**

```python
model = YOLO("models/yolov8n.pt")
model.train(data="data/lane/data.yaml", epochs=50)
```

**4. 拿新模型检测**

---

## 2026-09-23（标注 + 训练出自己的第一个模型）

### 一、学到了什么

**1. 标注工具 LabelImg**

- 启动：桌面 `标注车道线.bat`，双击即可（不用记命令）
- 三个参数：图片目录、类别清单文件、保存目录
- 快捷键：`W` 画框，`D` 下一张，`A` 上一张，`Delete` 删框
- `classes.txt` 的作用：画框时直接选类别，**避免手打类别名打错**（打成 `Lane` 或 `lane ` 会被当成两个类）

**2. 两种标注格式的区别**

| 格式 | 坐标长什么样 |
|---|---|
| PascalVOC（`.xml`） | `xmin=796 ymin=468 xmax=1932 ymax=1097`（像素绝对值） |
| YOLO（`.txt`） | `0 0.706004 0.611328 0.587992 0.491406`（归一化到 0~1） |

换算公式：

```
x_center = (xmin + xmax) / 2 / 图片宽
y_center = (ymin + ymax) / 2 / 图片高
width    = (xmax - xmin) / 图片宽
height   = (ymax - ymin) / 图片高
```

**3. 标注策略（定了就不改）**

- **每个看得清的白色线块 = 一个框**（实线一整条 1 个框，虚线每段 1 个框）
- 看不清的不标、被车挡死的不标、影子水渍不标
- 一张图 3~10 个框
- **一致性比"贴合完美"重要** —— 模型学的是你的标准

**4. 为什么要留验证集**

训练集用来学，验证集用来考。**用没见过的图考，才知道是真学会还是背答案。**

本次划分：**23 张训练 / 5 张验证**。

**5. 训练是一条完整链路**

```
收集图片 → 标注 → 格式转换 → 划分 train/val → 写 data.yaml → 训练
```

**6. 第一次训练的结果**

- 50 轮，**4 分 22 秒**（CPU）
- `box_loss` 2.03 → **1.48**，`cls_loss` 3.50 → **1.52**
- **mAP50 = 0.748**（74.8 分），精确率 0.874，召回率 0.536
- 产物：`output/lane_train/weights/best.pt`

**7. 训练原理（详细版单独写成 `模型训练笔记.md`）**

- **epoch（轮）** = 把训练集看一遍。50 轮 = 看 50 遍（不是"学了 50 张"）
- **loss** = 错误分数，越小越好
- **mAP** = 考试成绩，0~1
- **迁移学习**：从 `yolov8n.pt` 接着学。继承"怎么看图"的能力，输出层被换成只有 1 类 → 所以新模型不认人车狗了
- **参数（旋钮）= 权重**：yolov8n 有 **315 万**个；数量 = 输入 × 输出（**乘法**）
- 顺带解释：yolov8m 卡、yolov8n 流畅，是因为参数少 8 倍

**8. src 目录分类**

16 个脚本 → 4 个文件夹（`01_CV基础` / `02_图片检测` / `03_视频处理` / `04_数据集与训练`），
并写了 `src/README.md` 说明每个脚本干什么。

---

### 二、踩过的坑

| 坑 | 正确做法 |
|---|---|
| `cv2.imread` 读不了中文路径 | 用 `np.fromfile` + `cv2.imdecode`（OpenCV 内部走 C++ `fopen`，不认中文） |
| LabelImg 报 `Permission denied` | 第二个参数要的是**类别文件**，不是保存目录。参数顺序：图片 / 类别文件 / 保存目录 |
| LabelImg 鼠标一动就闪退 | 老软件（2020 停更）+ 新版 PyQt5 不兼容，给坐标加 `int()`（改了 7 处） |
| LabelImg 存成 `.xml` | 左侧 `PascalVOC` 按钮点一下切成 `YOLO` |
| bat 文件里写中文导致乱码 | **bat 内容只用英文**（ascii），文件名可以是中文 |
| 训练结果跑到 `runs/detect/` 下了 | ultralytics 的 `project` 参数**必须写绝对路径** |

---

### 三、备忘

- **运行纪律**：永远在 `D:\yolo-practice` **根目录**敲命令，别 `cd` 进 src 里跑
- 标注工具：双击桌面 `标注车道线.bat`
- 我的模型：`output/lane_train/weights/best.pt`
- 训练原理详解：`模型训练笔记.md`
- 脚本说明：`src/README.md`
- 新功能一律**新建文件**，不改旧的

---

### 四、下一步

**1. 用自己训的模型跑视频**

```
python src\03_视频处理\video_play_lane.py
```

**2. 28 张数据太少**，效果想更好必须加数据（CULane 有 13 万张）

**3. 模型导出 ONNX → 端侧部署**（对齐就业方向）

---

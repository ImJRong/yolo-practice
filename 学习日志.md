# YOLO + OpenCV 学习笔记


---

## 目录

1. [OpenCV：图片就是数字表格](#1-opencv图片就是数字表格)
2. [YOLO：检测一张图](#2-yolo检测一张图)
3. [视频处理](#3-视频处理)
4. [训练自己的模型](#4-训练自己的模型)
5. [部署：ONNX / 量化 / 推理](#5-部署onnx--量化--推理)
6. [踩坑速查表](#6-踩坑速查表)
7. [命令与文件速查](#7-命令与文件速查)

---

## 1. OpenCV：图片就是数字表格

### 1.1 核心认知

图片不是图片，是一张巨大的数字表格。每个格子一个像素，3 个数 = 蓝、绿、红。

```python
import cv2
img = cv2.imread("bus.png")
print(img.shape)   # (315, 474, 3) = 高 315 行、宽 474 列、每格 3 个数
print(img.dtype)   # uint8 = 每个数 0~255，占 1 字节
print(img[0, 0])   # [203 189 191] = 蓝 203、绿 189、红 191
```

### 1.2 索引顺序：`img[行, 列]` = `img[y, x]`

```
第一个位置 = 行 = 上下方向 = y
第二个位置 = 列 = 左右方向 = x
```

⚠️ YOLO 给的是 `(x, y)`，跟 numpy 索引**反着**。

```python
img[0, 400]    # 第 0 行、第 400 列 → 上边缘靠右
img[400, 0]    # ❌ 行只有 315，越界
```

### 1.3 BGR 不是 RGB

OpenCV 历史遗留，用 **BGR** 顺序（`img[0,0]` 第一个数是蓝色）。用 matplotlib 显示会红蓝颠倒。

### 1.4 切片（取一块区域）

```python
img[100:105, 200:205, 0]   # 第100~104行、第200~204列、蓝色通道
```

- **左闭右开**：`100:105` 取 5 个，不含 105
- 起点必须小于终点
- 第三个下标选通道：`0`=蓝 `1`=绿 `2`=红

### 1.5 常用函数

```python
cv2.resize(img, (宽, 高))                  # 缩放。⚠️ 参数是 (宽,高)，和 shape 的 (高,宽) 反着
cv2.imwrite("out.png", img)               # 保存。返回 True/False，失败不报错
cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)     # 转灰度
cv2.rectangle(img, (x1,y1), (x2,y2), (0,0,255), 5)   # 画矩形，坐标是 (列,行)
cv2.putText(img, "字", (x, y-8), cv2.FONT_HERSHEY_SIMPLEX, 0.6, (0,0,255), 2)  # 写字，坐标是左下角
```

### 1.6 中文路径（重要坑）

`cv2.imread` / `cv2.imwrite` 内部走 C++ 的 `fopen`，**不认中文路径**。改用：

```python
import numpy as np
import cv2

raw = np.fromfile("中文路径/图.jpg", dtype=np.uint8)   # 按字节读整个文件
img = cv2.imdecode(raw, cv2.IMREAD_COLOR)              # 从字节流解码成图

ok, buf = cv2.imencode(".jpg", img)                    # 写也一样：编码成字节流
buf.tofile("中文路径/图.jpg")                          # 直接写盘
```

### 1.7 灰度图怎么来的

每格 3 个数压成 1 个：

```
灰度 = 0.299×R + 0.587×G + 0.114×B
```

绿权重最高（人眼对绿色最敏感）。结果 shape 从 `(315,474,3)` 变 `(315,474)`。

---

## 2. YOLO：检测一张图

### 2.1 最小代码

```python
from ultralytics import YOLO

model = YOLO("yolov8n.pt")     # 加载模型
results = model("图片.jpg")     # 检测

for r in results:
    for box in r.boxes:
        print(model.names[int(box.cls)])   # 类别名，如 person
        print(float(box.conf))             # 置信度 0~1
        print(box.xyxy)                    # 框坐标
```

### 2.2 结果层级

```
results    列表，装所有图片的结果
  └ r      一张图的结果
      └ r.boxes    这张图里所有的框
          └ box    其中一个框
```

### 2.3 常用属性

| 写法 | 意思 |
|---|---|
| `model.names[int(box.cls)]` | 类别名 |
| `float(box.conf)` | 置信度，越接近 1 越确定 |
| `box.xyxy` | 左上+右下 `[x1,y1,x2,y2]` |
| `box.xywh` | 中心+宽高 `[cx,cy,w,h]` |
| `box.xyxyn` | 归一化版（0~1） |
| `box.cls` | 类别编号 |

### 2.4 调参

```python
model("图.jpg", classes=[0])   # 只检测人（0 号类）
model("图.jpg", conf=0.5)      # 只留置信度 0.5 以上的框
model("图.jpg", conf=0.5, iou=0.4)   # iou 管去重：越低删得越狠
```

- `classes` 想看有哪些编号：`print(model.names)`
- `conf` 调高 = 误检少但漏检多，调低反之
- `iou`（交并比）= 两个框重叠面积 ÷ 总面积，默认 0.7，调低去重更狠
- 常见类别编号：0 person / 2 car / 5 bus

### 2.5 张量剥壳

`box.cls` 打印是 `tensor(0.)`，不是普通数字，要先转：

```python
int(box.cls)       # 变成 0，才能写 model.names[0]
float(box.conf)    # 变成 0.87，才能计算
```

- 只有一个数的张量能直接转；多个数的用 `.tolist()`
- 规律：看 `.shape` 有几个数，就剥几次。`(1,4)` → 剥一次 → `(4,)`
- `box.xyxy` 是 `[[x1,y1,x2,y2]]` 两层括号，取单个要 `box.xyxy[0][0]`

### 2.6 YOLO 传参：支持文件路径，也支持图片数组

```python
model("data/car.png")   # 传文件路径，它自己读文件
model(img)              # 传已读好的 numpy 数组，省一次读取
model(frame)            # 视频逐帧，传的就是数组
```

---

## 3. 视频处理

### 3.1 读视频 + 读信息

```python
cap = cv2.VideoCapture("data/test.mp4")
w   = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))    # 宽
h   = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))   # 高
fps = cap.get(cv2.CAP_PROP_FPS)                 # 帧率
```

### 3.2 抽帧：`%` + `continue`

```python
if n % 10 != 0:      # n 不是 10 的倍数
    n = n + 1        # ⚠️ 递增必须写在 continue 之前
    continue         # 跳过本轮剩下代码
```

- `%` 取余数。效果：只处理 0,10,20... 帧
- 为什么抽帧：相邻帧几乎一样，全跑太慢，抽着看够用

### 3.3 写视频三件套

```python
fourcc = cv2.VideoWriter_fourcc(*"mp4v")
out = cv2.VideoWriter("output/out.mp4", fourcc, fps, (w, h))   # (w,h) 是宽高
out.write(frame)
out.release()    # 必须调，不然文件是坏的
```

⚠️ `mp4` 是容器不是编码。`mp4v` 编码 Windows 自带播放器放不了，换 PotPlayer/VLC 或用 `avc1`（H.264）。

### 3.4 实时播放（不存文件）

```python
cv2.imshow("video", frame)                    # 弹窗显示这一帧
if cv2.waitKey(1) & 0xFF == ord("q"):         # 按 q 退出
    break
cv2.destroyAllWindows()                       # 循环结束关窗口
```

`cv2.waitKey(1)` 拆开看：

| 部分 | 意思 |
|---|---|
| `cv2.waitKey(1)` | ①让画面刷新 ②检查这 1ms 有没有按键 |
| `& 0xFF` | 只留最低 8 位，屏蔽杂数据（跨平台保险） |
| `== ord("q")` | ord 把字符转数字，q → 113 |

参数 `1` = 等 1 毫秒；给 `0` 会一直卡住等按键。

### 3.5 模型选型（端侧部署核心权衡）

| 模型 | 速度 | 精度 | 参数量 |
|---|---|---|---|
| yolov8n | 快 | 略低 | 315 万 |
| yolov8s | 中 | 中 | 1116 万 |
| yolov8m | 慢 | 高 | 2590 万 |

CPU 上 v8m 一帧约 200ms，视频要 33ms/帧，所以卡。**换小模型 = 牺牲精度换速度。**

---

## 4. 训练自己的模型

### 4.1 完整链路

```
收集图片 → 标注 → 格式转换 → 划分 train/val → 写 data.yaml → 训练
```

### 4.2 目录结构（YOLO 约定）

```
data/lane/
├── images/   放图片
└── labels/   放标注文件
```

文件名一一对应：`images/lane_01.jpg` ↔ `labels/lane_01.txt`

### 4.3 标注（LabelImg）

- 启动：桌面 `标注车道线.bat`，双击即可
- 快捷键：`W` 画框，`D` 下一张，`A` 上一张，`Delete` 删框
- 三个参数：图片目录、类别清单文件（`classes.txt`）、保存目录
- **必须把格式从 PascalVOC 切成 YOLO**（左侧按钮），否则存 .xml

**两种标注格式：**

| 格式 | 坐标长什么样 |
|---|---|
| PascalVOC（`.xml`） | 像素绝对值 `xmin ymin xmax ymax` |
| YOLO（`.txt`） | 归一化到 0~1 |

换算公式：

```
x_center = (xmin + xmax) / 2 / 图片宽
y_center = (ymin + ymax) / 2 / 图片高
width    = (xmax - xmin) / 图片宽
height   = (ymax - ymin) / 图片高
```

**标注策略（定了就不改）：**
- 每个看得清的目标一个框；看不清/被挡死/影子水渍不标
- **一致性比贴合完美重要**——模型学的是你的标准

### 4.4 训练 / 验证集

训练集用来学，验证集用来考。**用没见过的图考，才知道是真学会还是背答案。** 本次 23 训练 / 5 验证。

### 4.5 训练概念（详细版见 `模型训练笔记.md`）

| 概念 | 意思 |
|---|---|
| epoch（轮） | 把训练集看一遍。50 轮 = 看 50 遍 |
| loss | 错误分数，**越小越好**，训练时看它下降 |
| mAP50 | 考试成绩 0~1，**越大越好** |
| 迁移学习 | 从 `yolov8n.pt` 接着学，继承"看图能力"，输出层换成 1 类 |
| 参数（权重） | 模型的旋钮。yolov8n 有 315 万个 |

**训练代码：**

```python
from ultralytics import YOLO

model = YOLO("models/yolov8n.pt")   # 预训练模型（迁移学习起点）
model.train(
    data="data/lane/data.yaml",
    epochs=50,            # 先跑 10 快速看结果
    imgsz=640,
    batch=4,
    device="cpu",         # 你没显卡
    workers=0,            # Windows 多进程易报错，设 0 更稳
    project=r"D:\yolo-practice\output",   # 必须绝对路径，否则跑到 runs/detect/
    name="lane_train",
)
```

**怎么读训练结果：**
- 只看两件事：`mAP50`（越大越好）和 loss 曲线有没有往下走
- **原始 loss 数字不用记**，那只是中间过程
- 我的结果：v8n 在 28 张图上 mAP50 = 0.748。数据太少，泛化差，正常

---

## 5. 部署：ONNX / 量化 / 推理

### 5.1 ONNX = 模型的通用格式

`best.pt` 是 PyTorch 私有格式，别的软件读不了；ONNX 是谁都能读的"通用插头"。

**导出（一行命令）：**

```
yolo export model=output/lane_train/weights/best.pt format=onnx imgsz=640
```

- `imgsz=640` 必须和训练时一致
- 产物 `best.onnx`

### 5.2 量化 = 把数字压小

fp32（4 字节）→ int8（1 字节），体积省 72%。

```
yolo export model=best.pt format=onnx int8=True data=data.yaml
```

- `int8=True` 开量化，`data=` 给图做标定（摸清数字范围再压）
- 产物 `best_int8.onnx`
- **最重要教训：量化≠一定更快。** 它提速的前提是硬件有 NPU（专门算 int8 的芯片）。普通 CPU 没有，反而更慢。是给 RK3588 那种板子准备的。

### 5.3 推理三段：预处理 → 推理 → 后处理

一张图从进去到出框，走三个车间：

| 阶段 | 干什么 | 谁决定的 |
|---|---|---|
| 预处理 | 缩到 640、BGR→RGB、压到 0~1 | 你写，部署岗天天写 |
| 推理 | `session.run(...)` 真正算 | 模型决定，最耗时 |
| 后处理 | 从 8400 个候选框里筛分数、去重 | 你写 |

```python
import onnxruntime as ort
session = ort.InferenceSession("best.onnx", providers=["CPUExecutionProvider"])
out = session.run(None, {session.get_inputs()[0].name: x})[0]
```

输入 `[1,3,640,640]`，输出 `[1,5,8400]`（单类：4 坐标 + 1 分数，8400 个候选框）。

### 5.4 实测速度（我的电脑，仅供参考）

| 引擎 | 纯推理 | FPS | 体积 |
|---|---|---|---|
| PyTorch (.pt) | 46.4 ms | 21.5 | 6.2 MB |
| ONNX fp32 | 21.4 ms | 46.7 | 11.7 MB |
| ONNX int8 | 43.0 ms | 23.3 | 3.2 MB |

结论：**ONNX 引擎比 PyTorch 快 2 倍；int8 在 CPU 上反而慢一倍**（等上板子才有用）。

---

## 6. 踩坑速查表

| 坑 | 正确做法 |
|---|---|
| 终端报 `No module named 'ultralytics'` | 看提示符有没有 `(yolo)`，没有就是环境没激活 |
| `cv2.imread` 读中文路径失败 | 用 `np.fromfile` + `cv2.imdecode` |
| `imread` 读不到文件不报错，返回 None | 加 `if img is None: exit()` |
| 图片明明在却读不到 | 相对路径是相对于"敲命令的目录"，用绝对路径或先 cd |
| `img[400,300]` 越界 | 先看 `img.shape`，行数别超 |
| 切片 `y1:y1-5` | 起点必须小于终点，应 `y1:y1+5` |
| 画框 `img[x1:x2, y1:y2]` 画歪 | 第一个位置是行(y)，应 `img[y1:y2, x1:x2]` |
| `int(box.xyxy[0])` 报错 | xyxy 是 4 个数，要 `int(box.xyxy[0][0])` |
| 抽帧 `continue` 前忘递增 | `n = n + 1` 写在 continue 之前 |
| 存图写 `img = cv2.imwrite(...)` | 返回的是 True/False，不是图片 |
| 换图忘了改 `classes` | bus→`[5]`，car→`[2]` |
| mp4 点不开 | mp4 是容器，编码 `mp4v` 换播放器，或改 `avc1` |
| `VideoWriter` 输出目录不存在 | 静默失败生成空文件，目录要手动建 |
| LabelImg 报 Permission denied | 第二参数是类别文件不是目录，顺序：图片/类别/保存目录 |
| LabelImg 鼠标一动就闪退 | 老软件 + 新版 PyQt5 不兼容，坐标加 `int()` |
| LabelImg 存成 .xml | 左侧 PascalVOC 按钮切成 YOLO |
| bat 里写中文乱码 | bat 内容只用英文 |
| 训练结果跑到 runs/detect/ | `project` 参数必须写绝对路径 |
| int8 导出报错去下载 coco8.zip | 加 `data=data/lane/data.yaml` 用自己的图标定 |
| int8 导出删掉 fp32 的 best.onnx | 需要就重新导出 fp32 一次 |

---

## 7. 命令与文件速查

### 运行纪律

- 永远在 `D:\yolo-practice` **根目录**敲命令，别 cd 进 src 里跑
- 打开终端：双击 `start-yolo.bat`（自动激活 yolo 环境 + 切目录）
- 清屏 `cls`；关终端 `exit`

### 命令格式统一记法

```
yolo 干什么 参数1=值 参数2=值
```

"干什么"换成 train / predict / val / export。

### 关键文件

| 文件 | 是什么 |
|---|---|
| `models/yolov8n.pt` | 预训练模型（迁移学习起点） |
| `output/lane_train/weights/best.pt` | 我训的模型 |
| `output/lane_train/weights/best.onnx` | 导出的 fp32 版（现在用这个） |
| `output/lane_train/weights/best_int8.onnx` | 量化版（等上板子再用） |
| `data/lane/data.yaml` | 训练数据配置单 |
| `src/05_部署/benchmark_onnx.py` | 测单张图速度 |
| `src/05_部署/video_onnx.py` | ONNX 视频实时检测 |

### 完整工程流程 9 步

```
数据 → 标注 → 训练 → 评估 → 导出 ONNX → 量化 → 推理 → 真机 → 迭代
```

前 7 步已走完。剩下的：真机部署（需买板子 + Linux）、迭代优化（加数据重训，这是常态循环）。

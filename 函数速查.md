# OpenCV + YOLO 函数速查

> 记住一条主线，所有代码都是它的排列组合：
> **读进来 → 检测 → 画框 → 存/塞出去**

两个库分工明确，各管一摊，谁也别越界。

---

## OpenCV —— 负责「读、画、存」（跟文件/画面打交道）

| 函数 | 作用 | 返回 |
|---|---|---|
| `cv2.imread(路径)` | 读图片 | numpy 数组 |
| `cv2.imwrite(路径, 图)` | 存图片 | True / False |
| `cv2.VideoCapture(路径)` | 打开视频 | 视频对象 cap |
| `cap.read()` | 读一帧 | `ret, frame` |
| `cap.get(常量)` | 查宽/高/帧率 | 数字 |
| `cap.isOpened()` | 视频打开成功没 | True / False |
| `cap.release()` | 关视频 | — |
| `cv2.VideoWriter(名, 编码, 帧率, 尺寸)` | 建视频容器 | writer |
| `cv2.VideoWriter_fourcc(*"mp4v")` | 选压缩方法 | 编码编号 |
| `writer.write(帧)` | 塞一帧进容器 | — |
| `writer.release()` | 封口（必须调） | — |
| `cv2.rectangle(...)` | 画框 | — |
| `cv2.putText(...)` | 写字 | — |
| `cv2.resize(...)` / `cv2.cvtColor(...)` | 缩放 / 转灰度 | numpy 数组 |

## YOLO —— 负责「检测」（只负责认东西）

| 函数 / 属性 | 作用 | 返回 |
|---|---|---|
| `YOLO("模型.pt")` | 加载模型 | model |
| `model(图/视频)` | 检测 | **列表** results |
| `results[0]` | 取第一个结果 | 结果对象 r |
| `r.boxes` | 所有检测框 | 框集合 |
| `box.xyxy` | 框坐标 [x1,y1,x2,y2] | 张量 |
| `box.cls` | 类别编号 | 张量 |
| `box.conf` | 置信度 | 张量 |
| `model.names[编号]` | 编号 → 名字 | 字符串 |
| `r.plot()` | 返回画好框的图 | numpy 数组 |
| `r.save(路径)` | 画好框并直接存 | — |

---

## 完整流水线（图片）

```python
img = cv2.imread("data/bus.png")   # OpenCV 读
r   = model(img)[0]                 # YOLO 检测
out = r.plot()                      # YOLO 画好框
cv2.imwrite("out.png", out)         # OpenCV 存
```

## 完整流水线（视频）

```python
cap = cv2.VideoCapture("data/x.mp4")          # OpenCV 打开视频
writer = cv2.VideoWriter(...)                 # OpenCV 建容器

while True:
    ret, frame = cap.read()                   # OpenCV 读一帧
    if not ret: break
    frame = model(frame)[0].plot()            # YOLO 检测+画框
    writer.write(frame)                       # OpenCV 塞进容器

cap.release(); writer.release()               # OpenCV 收尾
```

---

## 怎么分辨 / 记住

**看前缀：**
- `cv2.xxx` → OpenCV → 读写画
- `model.xxx` / `box.xxx` / `r.xxx` → YOLO → 检测

**两个易混点：**

1. **列表 vs 张量**
   - `model()` 返回**列表** → 用 `[0]` 取
   - `box.xyxy` 等是**张量** → 用 `int()` / `.tolist()` 转

2. **plot vs save vs write**
   - `r.plot()` → 拿到画好的图（numpy 数组）
   - `r.save(路径)` → 画好 + 直接存文件
   - `writer.write(帧)` → 塞进视频容器

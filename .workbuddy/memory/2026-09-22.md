# 2026-09-22

## YOLO + OpenCV 学习续接（场景：日常开发）
- 通过 task-chat-history 回溯历史：主会话在 `c-Users-RONGG-WorkBuddy-2026-09-21-09-56-44`（标题"将Reasonix快捷方式放到桌面"，实际是学习主线）。
- 已确认环境正常：yolo 环境 opencv 5.0.0 / torch 2.14.0+cpu / ultralytics 8.4.157 / numpy 2.4.6。
- 项目现状：`src/` 10 个 py（已重命名为语义化名字）、`data/`、`models/`（4 个 pt）、`output/`（含 `video_yolo/` 抽帧 31 张）、`学习日志.md`（9-21 两节 + 9-22 一节）、`代码讲解_视频与画框.md`、桌面 `YOLO学习速查.md`。
- 进度：单图检测 → OpenCV 基础（索引/BGR/切片/画框）→ 多图批处理 → 视频抽帧 → 视频逐帧检测（两种写法：`r.plot()` 自动画框版 + `cv2.rectangle` 手动画框版）。
- 上次中断点：刚学完视频检测，下一步原计划「1. 视频实时播放版（imshow + waitKey）2. 训练自己的模型（LabelImg → data.yaml → model.train）」。

## 用户协作偏好（重要，反复强调过）
- 回复要短，**一次别超过 100 字**，看不懂长文。
- 别写脚本、别自作主张做额外的事；只回答问题。
- 代码要**展开写**，不用列表推导式（看不懂）。
- 别画蛇添足（例如反复确认文件能不能打开）。
- 讲解要详细但**不要太口语化**，别炫技。

## 18:37 进度：视频实时播放版
- 新建 `src/video_play.py`（第 7 项）—— `cv2.imshow` + `cv2.waitKey(1) & 0xFF == ord("q")` + `cv2.destroyAllWindows()`。
- 保留 `r.plot()` 自动画框写法，去掉 `VideoWriter`，按 q 退出。
- 预期现象：CPU 跑 yolov8m 约 200ms/帧，播放会卡 → 引出"端侧部署/推理加速"话题（对应就业方向）。
- 待用户实跑反馈。

## 20:45 进度：视频实时播放收尾 + 开始第 8 项（训练自己的模型）
- 用户反馈 yolov8m 卡 → 换 `yolov8n.pt` 好多了。已讲清"模型选型权衡"就是端侧部署核心。
- `src/video_play.py` 重写为**通用版**：配置区集中（VIDEO/MODEL/MAX_W/MAX_H），
  `min(MAX_H/h, MAX_W/w)` 取较小比例自动适配横竖屏，只在需要时 resize。
- 小插曲：`imshow` 窗口显示不全，我最初归因于尺寸，用户质疑"其他视频怎么没事"——
  已承认是猜测，正解是 `imshow` 窗口尺寸由首次调用决定、之后不再变。用户表示不重要，跳过。
- 讲清了 `waitKey(1) & 0xFF == ord("q")` 三层含义（刷新画面/抓键盘、屏蔽高位、字符转编码）。

### 数据源调研结论（车道线方向）
- **TuSimple**：~10GB，AWS 直链无需注册（`https://s3.us-east-2.amazonaws.com/benchmark-frontend/datasets/1/train_set.zip`），
  高速白天简单场景，新手首选。
- **CULane**：~40GB，官网 `https://xingangpan.github.io/projects/CULane.html`，
  国内镜像 `https://openxlab.org.cn/datasets/OpenDataLab/CULane`；133235 帧、1640×590、8 类挑战场景。
  **导师师兄（崔子琦）论文用的就是这个**。
- ⚠️ 关键坑：这两个数据集标注是**车道线折线（polylines）**，不是 YOLO 的矩形框，直接拿来训 YOLO 必须先写转换脚本。
- 建议路径：先自己拍 50-80 张马路照片手标（学标注本质）→ 再上数据集做格式转换（进阶）。

### 环境部署（已完成）
- 新建目录 `data/lane/images/` 与 `data/lane/labels/`（YOLO 靠同名文件对应，images/xxx.jpg ↔ labels/xxx.txt）。
- 装了 `labelImg 1.8.6` + `PyQt5 5.15.2`（在 yolo 环境，exe 在 `D:\miniconda3\envs\yolo\Scripts\labelImg.exe`）。
- 启动命令：`labelImg D:/yolo-practice/data/lane/images D:/yolo-practice/data/lane/labels`
- 使用要点：左侧 PascalVOC 按钮切成 **YOLO**（存 .txt）；勾 `View → Auto Save Mode`。
- D 盘剩余 196G，够用。

## 21:12 进度：车道线数据规整（完成）
- 用户自己从网上下了 28 张车道图放在 `桌面\车道图\`（含 jpg/webp/png 混合、尺寸 475×633 ~ 4096×3072）。
- ⚠️ **踩坑（重要）**：`cv2.imread` **读不了中文路径**（`C:\Users\RONGG\Desktop\车道图\xxx.jpg`），
  报 `cv2::findDecoder can't open/read file`，28 张全部失败。
  **正解**：`raw = np.fromfile(path, dtype=np.uint8)` → `img = cv2.imdecode(raw, cv2.IMREAD_COLOR)`；
  写盘同理 `ok, buf = cv2.imencode(".jpg", img)` → `buf.tofile(path)`。
  原因：OpenCV 的 imread/imwrite 内部走 C++ fopen，不走 Windows Unicode API，中文路径必挂。
- 脚本：`src/dataset_fix_lane.py`（读 `C:\Users\RONGG\Desktop\车道图`，输出到 `data/lane/images/`）。
- 结果：28 张全部成功，统一转 .jpg 并重命名为 `lane_01.jpg` ~ `lane_28.jpg`，共 14MB。
- 尺寸分布：<1080p 9 张 / 约 1080p 12 张 / >2000px 7 张（最大 4096×3072）。YOLO 会自动缩放到 imgsz，不影响训练。
- 下一步：用 LabelImg 标注。启动命令 `labelImg D:/yolo-practice/data/lane/images D:/yolo-practice/data/lane/labels`，
  必须先切 **YOLO** 格式（默认 PascalVOC 会存 .xml，YOLO 不认），勾 Auto Save Mode，按 W 画框。
- 待办：教标注规范（框到哪、一张图几条线、遮挡怎么办）。

# 代码讲解：视频抽帧 + 画框两版

> 只讲新东西。已经学过的（`xyxy[0][0]`、`rectangle`、`putText`）一带而过。

---

## 第一份：`src/video_yolo.py` —— 视频抽帧存图

你已经跑过了，`output/vedio_yolo/` 里那 31 张 `video_0.jpg` ~ `video_30.jpg` 就是它生成的。

### 新增知识点只有 1 个：`n % 10 != 0` + `continue`

```python
n = 0          # 第几帧
saved = 0      # 存了几张图

while True:
    ret, frame = cap.read()
    if not ret:
        break

    # ← 关键在这四行
    if n % 10 != 0:
        n = n + 1
        continue

    ... 处理这一帧 ...
    n = n + 1
```

**`%` 是取余数**（不是百分号）：

```
1 % 10 = 1      11 % 10 = 1
5 % 10 = 5      20 % 10 = 0
10 % 10 = 0     305 % 10 = 5
```

`n % 10 != 0` 的意思是"**帧号不是 10 的整数倍**"，也就是 1,2,3...9,11,12...19...

**`continue` = 跳过本轮剩下的代码，直接进入下一轮循环。**

所以这三行的效果是：帧号不是 10 的倍数 → 立刻跳到下一帧，**不检测、不画框、不存图**。

于是只有 n = 0, 10, 20, 30 ... 300 这 **31 帧**被处理。

### 为什么要抽帧？三个理由

| 理由 | 说明 |
|---|---|
| **省时间** | 305 帧全跑，CPU 要 70~90 秒；抽帧后只跑 31 帧，**约 9 秒** |
| **够用** | 30fps 的视频，相邻帧几乎一样。看"有没有车""车在哪"完全够 |
| **省硬盘** | 305 张 1080×1920 的 jpg ≈ 300MB，抽帧后 31 张 ≈ 30MB |

### `n = n + 1` 为什么出现两次？

因为 `continue` **会跳过它后面所有的代码**。如果只在末尾写一次 `n = n + 1`：

```python
if n % 10 != 0:
    continue      # ← 跳过了末尾的 n = n + 1
...
n = n + 1
```

那么跳过的帧，`n` 根本不会增加 → `n` 永远等于 1 → **死循环**（`1 % 10 != 0` 永远成立，每帧都被跳过，`cap.read()` 又一直能读到东西…… 实际上会一直读完 305 帧然后 `break`，但 `n` 永远是 1，只存了 1 张图）。

所以**跳过之前必须先给 `n` 加 1**。

### ⚠️ 循环结束后的两个收尾

```python
cap.release()
print("全部完成")
```

`video_yolo.py` 只有 `cap.release()`，没有 `out.release()` —— 因为**它不写视频，只存图片**。存图片用 `cv2.imwrite`，每存完就落盘了，不需要额外收尾。

`cap.release()` 是必须的：**告诉系统"这个视频文件我不用了，锁可以放开了"**。不写的话，这个视频文件在程序结束前一直被占用（想删删不掉、想覆盖覆盖不了）。

### 结尾多余的 `n = n + 1`

```python
    print(f"第 {n} 帧完成，已存 {saved} 张")

    n = n + 1      # ← 这个在存图之后，是对的
```

注意顺序：**先存图（用当前的 n）→ 再打印 → 最后才 n+1**。

如果写成先 `n = n + 1` 再存图，文件名就会从 `video_1.jpg` 开始，且 `n=0` 时打印的是"第 1 帧"。

你可以直接删掉这行末尾的 `n = n + 1` 试试，结果会变成：**所有视频文件都在覆盖同一张图**（因为每轮 `n` 又变成 0）。

---

## 第二份：`src/cv_draw+.py` —— 画框带标签（参数抽成变量版）

这份是你自己重构过的：把颜色、线宽、字号提到了文件顶部当变量，方便调。

### 新增知识点 1：文件顶部的"配置区"

```python
line_w = 1     # 线的粗细
word_w = 1     # 文字的粗细
word_s = 0.4   # 文字的字号
red  = (0,0,255)
blue = (255,0,0)
```

这段在**函数外面（全局）**，好处是：

> 想调线宽？改第 4 行一个数字就行，不用去翻 `cv2.rectangle(...)` 那一长串参数。

这就是"**魔法数字提出来**"的做法。写死了叫魔法数字（magic number），提出来叫配置。

**`word_s = 0.4` 的坑**：`putText` 的 `fontScale` 是**缩放系数不是像素高**。

- `0.4` → 大概 10 像素高
- `1.0` → 大概 26 像素高
- 视频里 1080 宽的图，**`0.4` 会小到看不清**，建议 0.8~1.0

另外 **`putText` 的字号跟你图片尺寸无关**，图片放大一倍，字不会跟着变大。所以 `car.png`（1280×720）用 0.4 还行，换成 4K 图就得调到 1.5。

### 新增知识点 2：`classes=[2]` 为什么是 car

```python
results = model("data/car.png", classes=[2])
```

COCO 数据集里 **car 的编号是 2**（不是 5，5 是 bus）。

上次 `bus.png` 用 `classes=[5]` 是对的，换成 `car.png` 还用 5 就会**零检测**（`no detections`）。

### 我实测了一份 `car.png` 的结果

```
检测到框数: 12
tensor([[252.03, 498.44, 522.24, 680.83]])   0.939
tensor([[1146.18, 396.63, 1339.14, 519.40]]) 0.931
tensor([[617.90, 488.33, 872.14, 731.31]])   0.926
...
```

12 辆车全检出来了，置信度 0.93 起步。**4 个数字的意思**：

```
[252.03, 498.44, 522.24, 680.83]
   x1       y1       x2      y2
 左边缘   上边缘   右边缘  下边缘
```

注意 `x2 = 522 > x1 = 252`，`y2 = 680 > y1 = 498`，大小关系天然成立。**如果画出来是反的，说明你取错了下标。**

### 新增知识点 3：`img_cv` 这个名字是多余的

```python
img_cv = cv2.imwrite("output/car_cv+.png", img)
```

`cv2.imwrite` 返回的是 **`True` 或 `False`**（存成功/失败），不是图片。这里 `img_cv` 装的是个布尔值，后面完全没用到。

**建议改成这两行的任一种：**

```python
ok = cv2.imwrite("output/car_cv+.png", img)   # 改名，意义明确
if not ok:
    print("保存失败了！")                       # 加判断，能发现路径错误
```

或干脆：

```python
cv2.imwrite("output/car_cv+.png", img)
```

### ⚠️ 一个隐患：读图读完没检查

```python
img = cv2.imread("data/car.png")
...
cv2.rectangle(img, ...)     # 如果 img 是 None，这里直接崩
```

**`cv2.imread` 读不到文件不报错，静默返回 `None`**。文件名打错、路径不对，都会得到一个 `None`，然后在画框时才炸出一堆看不懂的错。

**标准做法（每次读图都加）：**

```python
img = cv2.imread("data/car.png")
if img is None:
    print("读不到图片，检查路径！")
    exit()
```

### 一个小的重复：`results[0]` 和 `model(...)` 都想一想

```python
results = model("data/car.png", classes=[2])
result = results[0]
```

`results` 是**列表**（装每张图的结果），这里只有一张图，所以取 `[0]`。

**注意**：你已经有 `img = cv2.imread(...)` 读进来的数组了，但 YOLO 又自己读了第二遍文件（`model("data/car.png")`）。

想省一次硬盘读取，可以改成直接喂数组：

```python
results = model(img, classes=[2])
```

跟 `video_yolo.py` 里 `model(frame, verbose=False)` 一个道理 —— **YOLO 既能吃文件路径，也能吃 numpy 数组。**

---

## 两份代码对比

| | `video_yolo.py` | `cv_draw+.py` |
|---|---|---|
| 输入 | 视频（305 帧） | 单张图片 |
| 喂给 YOLO 的 | `frame`（数组） | `"data/car.png"`（路径） |
| 处理多少 | 每 10 帧 1 帧，共 31 次 | 1 次 |
| 输出 | 31 张 jpg | 1 张 png |
| 收尾 | `cap.release()` | 不用 |
| 关键新语法 | `%` 取余 + `continue` | 全局变量做配置 |

---

## 本次新增的坑（补进学习日志）

| 坑 | 正确做法 |
|---|---|
| 抽帧循环里 `continue` 前忘了 `n = n + 1` | `continue` 会跳过后面的代码，**递增要写在 `continue` 之前** |
| `cv_draw+.py` 存图写 `img_cv = cv2.imwrite(...)` | `imwrite` 返回 `True`/`False`，不是图片 |
| 换图后忘了改 `classes` | `bus.png` 用 `[5]`，`car.png` 要用 `[2]` |
| `fontScale` 当成像素高 | 它是缩放系数，`0.4` 约 10 像素，图大了要调大 |
| `mp4v` 编码的视频 Windows 自带播放器打不开 | 后缀 `.mp4` 是容器格式，里面的编码是 `mp4v`，两者独立。换 PotPlayer/VLC 播 |

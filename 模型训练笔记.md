# 模型训练笔记

> 2026-09-23 学的东西。从「训练到底是什么」讲到「参数是什么」。
> 这一份是原理，不是操作步骤。看不懂就回来翻。

---

## 〇、一句话主线

**训练 = 教一个婴儿认东西。**

后面所有名词，都是这一个过程的零件。

---

## 一、训练到底在干什么

### 四样东西对号入座

| 现实里的东西 | 项目里的东西 |
|---|---|
| 课本 | 28 张标注好的图（你画的框 = 老师在指认） |
| 婴儿 | `models/yolov8n.pt` |
| 上课的过程 | `python src/04_数据集与训练/train_lane.py` |
| 学成的那个婴儿 | `output/lane_train/weights/best.pt` |

### 那个"婴儿"其实是什么

它不是人，是**一个文件，里面装着 315 万个数字**。

这些数字决定它怎么看图。可以想象成**一个有 315 万个旋钮的控制面板**：

- 旋钮位置随机 → 它瞎猜
- 旋钮调到合适位置 → 它认得很准

**"训练"干的唯一一件事，就是调这些旋钮。**

每猜错一次，程序自动把旋钮拧一点点。看一张、猜一次、调一次，反复几千次。

---

## 二、三个必须搞懂的名词

### 1. epoch（轮）—— 不是"张数"

**1 轮 = 把训练集里的图，从头到尾看一遍。**

你 28 张图里，23 张当课本、5 张当考卷。所以：

```
1 轮  = 看完 23 张
50 轮 = 把这 23 张反复看 50 遍
        = 每张图被看了 50 次，总共 1150 次
```

**为什么反复看**：看一遍记不住。就像小孩认字要重复很多次。

**轮数在哪定**：

```python
epochs=50,     # 在 train_lane.py 里，可以改
```

太少学不会，太多会"死记硬背"（专业叫**过拟合**：把课本背下来，换新题就废）。

### 2. loss（损失）—— 这一轮错了多少

终端刷的这三个数字，全是"错误分数"：

| 名字 | 人话 |
|---|---|
| `box_loss` | 框的位置偏了多少 |
| `cls_loss` | 类别认错了多少 |
| `dfl_loss` | 框的精细度差多少 |

**数字往下掉 = 它在进步。** 你那次：2.03 → 1.48，就是"错误从 2 分降到 1.5 分"。

### 3. mAP —— 最后的考试成绩

50 轮学完，程序拿出 **5 张它没见过的图**考它，考出来的分数叫 mAP。

- 范围 0 ~ 1
- **0.748 = 100 分制里的 74.8 分**
- 越接近 1 越好

**为什么要用没见过的图考**：如果考题就是课本，它背答案就行，看不出真本事。

---

## 三、迁移学习：为什么不从零开始

### 这个做法有名字：迁移学习

**人话**：不是从零教一个婴儿，而是**请一位已经会认一万种东西的老师傅，只教他认「车道线」这一件事**。

### 能不能从头来？能，但不建议

改一行代码就能从头：

```python
model = YOLO("models/yolov8n.pt")    # 现在：接着老模型学
model = YOLO("yolov8n.yaml")         # 改成这样：从零开始
```

**但 28 张图从零训练，效果会非常差。**

原因：从零要学会的不只是"什么是车道线"，还要学会**"怎么看图"** —— 认边缘、认形状、认明暗。这套本事需要**几万到几十万张图**才能练出来。

| 方式 | 需要多少图 |
|---|---|
| 从零训练 | 几万 ~ 几十万张 |
| 迁移学习 | 几百张就有效果 |

### 实际应用也这样吗？—— 是的，几乎 100%

工业界做任何新任务都从预训练模型开始，因为：

- **省数据**：几万张 → 几百张
- **省时间**：几天 → 几分钟
- **效果更好**：站在巨人肩膀上

### ⚠️ 关键问题：新模型继承了什么，丢了什么

**答案分两半：继承了「怎么看图」的眼力，丢掉了「认人的能力」。**

因为 YOLO 内部是**两段**：

```
输入图片
  ↓
【前半段】看图、提取特征（边缘、形状、明暗）   ← 这部分「继承」了老模型
  ↓
【后半段】输出层：报出类别名字                ← 这部分「被换掉」了
  ↓
"lane"
```

| 能力 | 状况 |
|---|---|
| 看图、找边缘形状 | ✅ 继承了（老模型的功劳） |
| 说出"lane" | ✅ 新学的 |
| 说出"人""车""狗" | ❌ 说不出来了 |

**为什么不认人了？不是它忘了怎么认，是你把它的嘴封成了只能吐一个词。**

> 类比：一个会说 80 国语言的翻译，你只让他回答"这是不是车道线"。
> 他的眼力和语言能力都还在，只是现在只准他说这一个答案。

---

## 四、参数（旋钮）到底是什么

### 就是网上说的「百万参数」

**参数（parameter）= 权重（weight）= "旋钮"。**

`weights` 这个文件夹名就是"装着旋钮数值的文件"。

### 你的模型各有多少（实测）

| 模型 | 参数量 |
|---|---|
| **yolov8n** | **3,157,200**（315 万） |
| yolov8s | 11,166,560（1116 万） |
| yolov8m | 25,902,640（2590 万） |

### 哪来这么多？靠"乘法"

一层里 5 个格子接 4 个格子，**每个输入都要连到每个输出**：

```
5 × 4 = 20 条连线，每条线上 1 个旋钮 → 20 个参数
```

**旋钮数 = 输入个数 × 输出个数**，是**相乘**不是相加。

真实网络里层层叠加：

```
图片   3 个数字
  ↓ ×16      → 48 个旋钮
16 个   ↓ ×32      → 512 个旋钮
32 个   ↓ ×64      → 2,048 个旋钮
64 个   ↓ ×128     → 8,192 个旋钮
...
```

**YOLOv8n 有 70 多层**，一层层乘下来就是 315 万。

### 一个旋钮具体在干什么

它是**"加权求和"的系数**。

举例 —— **认一条竖边**。把一组旋钮设成 `[+1, +1, -1, -1]`（左边要亮、右边要暗）：

| 遇到的情况 | 计算结果 | 结论 |
|---|---|---|
| 左边亮、右边暗 | +1+1-1-1 = **大值** | ✅ 这里有竖边 |
| 一片均匀的灰色 | +1-1+1-1 = **0** | ❌ 这里啥也没有 |

**旋钮的数值，就代表"它在找什么图案"。**

训练干的事，就是找出最能认出车道线的那套数值。315 万个旋钮，每个调一点点，调几千次。

### 后缀 n / s / m / l / x 的意思

| 后缀 | 意思 | 参数量 |
|---|---|---|
| n | nano（极小） | 315 万 |
| s | small（小） | 1116 万 |
| m | medium（中） | 2590 万 |
| l | large（大） | 约 4370 万 |
| x | extra large（超大） | 约 6820 万 |

**旋钮越多 → 能认的东西越复杂 → 但也越慢、越吃内存。**

> 这也解释了为什么 yolov8m 卡成幻灯片、换 yolov8n 就流畅了 ——
> 旋钮少了 8 倍，算得就快 8 倍。

---

## 五、训练输出的一堆文件，只有 1 个有用

打开 `output/lane_train/` 会看到 20 个文件。**真正有用的只有一个**：

| 文件 | 有用吗 | 是什么 |
|---|---|---|
| **`weights/best.pt`** | ✅ **就这个** | 学成的模型，以后用它检测 |
| `weights/last.pt` | 备用 | 最后一轮的版本，一般不如 best |
| `results.csv` | 参考 | 50 轮的成绩单（Excel 能打开） |
| `results.png` | 参考 | 把成绩单画成曲线，给人看的 |
| `confusion_matrix.png` | 参考 | 它把什么认成了什么 |
| `train_batch0/1/2.jpg` | 参考 | 训练时喂进去的图，存档 |
| `val_batch0_pred.jpg` | 参考 | 验证集的预测效果 |
| `args.yaml` | 存档 | 记录训练时用的参数 |

**除了 `best.pt`，其他都是"过程留痕"** —— 像做菜时拍的备菜照片，删了也不影响模型使用。

### 记住这一句

```
我的车道线模型 = output/lane_train/weights/best.pt
```

---

## 六、你那次训练的成绩单

（2026-09-23，50 轮，用时 4 分 22 秒）

| 指标 | 数字 | 怎么理解 |
|---|---|---|
| box_loss | 2.03 → **1.48** | 框位置越来越准 |
| cls_loss | 3.50 → **1.52** | 类别越认越对 |
| **mAP50** | **0.748** | 综合 **74.8 分** |
| 精确率 P | **0.874** | 它框出来的，87% 是真的 |
| 召回率 R | **0.536** | 真有的框，它找到 54% |

**精确率远高于召回率**是正常的：数据少（只有 28 张），模型学得保守，"宁可漏、不乱报"。

**想提高召回率，唯一的办法是加数据。**

---

## 七、怎么看 results.png（训练曲线）

`results.png` 里有 **10 个小格子**，每个格子一条曲线 ——
就是 `results.csv` 里 10 列数据画出来的。

### 先记一句话：看它是"该涨"还是"该跌"

| 组 | 几条 | 该往哪走 |
|---|---|---|
| 训练集的错误（loss） | 3 条 | **往下** |
| 成绩指标 | 4 条 | **往上** |
| 验证集的错误（loss） | 3 条 | **往下** |

### 第一组：训练集的三个"错误分数"（往下好）

| 图上英文 | 拆词 | 人话 |
|---|---|---|
| `train/box_loss` | train=训练集，box=框 | 它画的框跟答案差多远 |
| `train/cls_loss` | cls=classification（分类） | 类别认错的严重程度 |
| `train/dfl_loss` | dfl 是算法名 | 框的边界精不精细 |

### 第二组：四项成绩指标（往上好）

| 图上英文 | 拆词 | 人话 |
|---|---|---|
| `metrics/precision(B)` | metrics=指标 | **精确率**：它说有框的，多少是真的（别乱报） |
| `metrics/recall(B)` | | **召回率**：真有的框，它找到多少（别漏） |
| `metrics/mAP50(B)` | mAP=平均精度均值 | **综合得分**（框和答案重叠超 50% 就算对） |
| `metrics/mAP50-95(B)` | | **更严格的综合得分**（重叠度从 50% 到 95% 都算分取平均） |

### 第三组：验证集的三个"错误分数"（往下好）

| 图上英文 | 人话 |
|---|---|
| `val/box_loss` | 考卷上的框位置误差 |
| `val/cls_loss` | 考卷上的类别误差 |
| `val/dfl_loss` | 考卷上的框边界误差 |

`val` = validation（验证集），就是那 5 张考卷。

### 为什么要分 train 和 val 两套

- `train/*` = 它在**课本**上的表现
- `val/*` = 它在**考卷**上的表现

**关键用法**：如果 `train` 的 loss 一直降，但 `val` 的 loss 开始**往上翘**
→ 那就是"背答案"（过拟合），该停下来加数据了。

### 实操：10 条里只盯 3 条

```
train/box_loss    ← 要往下走
train/cls_loss    ← 要往下走
metrics/mAP50(B)  ← 要往上走
```

这三条走势对了，训练就是健康的。

---

## 八、英文缩写速查表

| 缩写 | 全称 | 中文 |
|---|---|---|
| box | bounding box | 检测框 |
| cls | classification | 分类 |
| dfl | Distribution Focal Loss | （算法名，理解成"框边界精细度"） |
| train | train | 训练集 |
| val | validation | 验证集 |
| metrics | metrics | 指标 |
| mAP | mean Average Precision | 平均精度均值 |
| P / precision | precision | 精确率 |
| R / recall | recall | 召回率 |
| (B) | Box | 检测框（区别于分割、姿态） |
| epochs | | 轮数（把所有图看几遍） |
| imgsz | image size | 喂给模型时的图片尺寸（640） |
| weights | | 权重 = 参数 = "旋钮" |
| best.pt / last.pt | | 最好的那一轮 / 最后一轮 |

---

## 九、还没学的（排队等你的，不是今天的作业）

| 名词 | 是什么 |
|---|---|
| **反向传播** | 旋钮具体"怎么被调的" —— 训练的核心算法 |
| **过拟合** | 背答案 vs 真学会，怎么判断、怎么防 |
| **ONNX** | 把模型换成通用格式，方便部署到别的设备 |
| **量化 / 剪枝** | 让模型变小变快的手段（端侧部署的核心） |

---


